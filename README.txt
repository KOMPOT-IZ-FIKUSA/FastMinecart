FastMinecart mod repository.
Changing the global minecart speed multiplier is possible with the config.
Possible values are fractions > 1. The mod can only speed up the minecarts. The mod speeds up every minecart of every type in every world.
